# TTY probes

Historical diagnostic captures, pasted into a live `iex` session by hand — not
runnable tests or examples. They are the recorded evidence behind the
`prim_tty` / SSH-attach design decisions referenced from
`docs/tty/tty-references.md` and the `Linx.Tty.Env` moduledoc.
